# Renji Research Lab — dependencies.
# The agent is built on Google ADK; the MongoDB MCP server runs over stdio (via npx).
# The Renji conscience is NOT here — it runs on our hosted heart, reached over HTTP.
google-adk>=1.2          # Gemini-3 LlmAgent + MCP toolset + before_tool_callback
mcp>=1.2                 # Model Context Protocol client (the partner MCP transport)
google-genai>=1.0        # Gemini (AI Studio) client used by ADK
httpx>=0.27              # the thin client to the hosted Renji heart
python-dotenv>=1.0       # (optional) .env loading; agent.py also loads .env itself
pymongo>=4.6   # guard verifies cited sources resolve in MongoDB
